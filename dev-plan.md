# 信息学竞赛结构元素开发计划

## 背景

当前“结构”功能是初版模板：数组、图、树会展开成普通 `rect`、`text`、`line`、`ellipse` 等白板基础元素，并通过 `groupId` 临时关联。这能快速插入图案，但不适合后续信息学竞赛教学。

主要问题：

- 保存文件里没有“数组/图/树”的语义，只有一堆基础图形。
- 数组无法天然支持插入、删除、交换、移动元素。
- 图无法表达有向边、边权、节点拖拽、边编辑、导入/导出边表等图编辑器能力。
- 树无法表达父子关系、子树、遍历、高亮和结构操作。
- 继续在普通图形组合上堆功能会让选择、拖拽、编辑、历史记录和保存兼容越来越脆弱。

## 已确认方向

- 将结构升级为一等元素类型，而不是普通图形组合。
- 新增三种结构元素：
  - `array-structure`
  - `graph-structure`
  - `tree-structure`
- 第一阶段先做结构化元素底座，不一次性实现完整编辑器。
- 后续图编辑目标参考 CS Academy Graph Editor 方向：支持有向/无向图、边权、节点拖拽、加点、连边、删边、输入/导出等。

## 第一阶段：结构化元素底座

目标：让白板文件和渲染层能够识别数组、图、树是结构元素，并保持当前插入、选择、移动、复制、撤销、保存、导出的基础能力。

范围：

1. 数据模型
   - `array-structure` 保存 `items`、尺寸和样式设置。
   - `graph-structure` 保存 `nodes`、`edges`、布局和默认图设置。
   - `tree-structure` 保存 `nodes`、父子关系或层序数据、布局设置。
2. 插入
   - 结构面板继续使用当前输入格式。
   - 插入时创建结构元素，不再展开成基础图形。
   - 默认插入到当前视口中心。
3. 渲染
   - Konva 渲染三种结构元素。
   - 第一阶段以整体对象渲染和变换为主。
4. 白板模型
   - `normalizeBoard`、`serializeBoard` 支持新类型。
   - 图层面板展示结构类型名称。
5. 基础交互
   - 整体选择、移动、缩放、复制、撤销、保存、导出继续可用。
   - 暂不做内部节点/元素编辑器。
6. 测试
   - 覆盖结构解析、结构元素创建、模型归一化、Konva 渲染和 UI 插入标记。

## 第二阶段：数组编辑器

目标：面向数组、序列、栈、队列等竞赛教学场景。

能力：

- 修改元素值。
- 在指定位置插入元素。
- 删除元素。
- 交换两个元素。
- 拖动元素重排。
- 支持高亮区间、指针标记、当前下标标记。
- 支持从逗号分隔文本重新导入。

## 第三阶段：图编辑器

目标：接近专用图编辑器能力，服务图论教学。

能力：

- 添加/删除节点。
- 拖动节点并保持边连接。
- 添加/删除边。
- 有向边和无向边。
- 边权。
- 自环和重边策略。
- 邻接矩阵、边列表、邻接表导入/导出。
- 图布局辅助：环形、分层、网格、保持手动布局。
- 高亮节点、边、路径、连通块。

## 第四阶段：树/二叉树编辑器

目标：支持二叉树、堆、BST、线段树等教学表达。

能力：

- 添加/删除节点或子树。
- 修改节点值。
- 自动层级布局。
- 左右儿子语义。
- 遍历顺序高亮。
- 支持层序数组导入和导出。
- 后续扩展 AVL/红黑树/堆等专用样式。

## 当前执行项

开始第一阶段：

1. 更新结构创建模块，让输入生成结构元素。
2. 扩展白板模型默认值和归一化逻辑。
3. 扩展 Konva 渲染结构元素。
4. 更新图层标签。
5. 更新测试并运行全量验证。
