# 信息学竞赛结构元素开发计划

## 背景

当前“结构”功能是初版模板：数组、图、树会展开成普通 `rect`、`text`、`line`、`ellipse` 等白板基础元素，并通过 `groupId` 临时关联。这能快速插入图案，但不适合后续信息学竞赛教学。

主要问题：

- 保存文件里没有“数组/图/树”的语义，只有一堆基础图形。
- 数组无法天然支持插入、删除、交换、移动元素。
- 图无法表达有向边、边权、节点拖拽、边编辑、导入/导出边表等图编辑器能力。
- 树无法表达父子关系、子树、遍历、高亮和结构操作。
- 继续在普通图形组合上堆功能会让选择、拖拽、编辑、历史记录和保存兼容越来越脆弱。

## 已确认方向

- 将结构升级为一等元素类型，而不是普通图形组合。
- 新增三种结构元素：
  - `array-structure`
  - `graph-structure`
  - `tree-structure`
- 第一阶段先做结构化元素底座，不一次性实现完整编辑器。
- 后续图编辑目标参考 CS Academy Graph Editor 方向：支持有向/无向图、边权、节点拖拽、加点、连边、删边、输入/导出等。

## 第一阶段：结构化元素底座

目标：让白板文件和渲染层能够识别数组、图、树是结构元素，并保持当前插入、选择、移动、复制、撤销、保存、导出的基础能力。

范围：

1. 数据模型
   - `array-structure` 保存 `items`、尺寸和样式设置。
   - `graph-structure` 保存 `nodes`、`edges`、布局和默认图设置。
   - `tree-structure` 保存 `nodes`、父子关系或层序数据、布局设置。
2. 插入
   - 结构面板继续使用当前输入格式。
   - 插入时创建结构元素，不再展开成基础图形。
   - 默认插入到当前视口中心。
3. 渲染
   - Konva 渲染三种结构元素。
   - 第一阶段以整体对象渲染和变换为主。
4. 白板模型
   - `normalizeBoard`、`serializeBoard` 支持新类型。
   - 图层面板展示结构类型名称。
5. 基础交互
   - 整体选择、移动、缩放、复制、撤销、保存、导出继续可用。
   - 暂不做内部节点/元素编辑器。
6. 测试
   - 覆盖结构解析、结构元素创建、模型归一化、Konva 渲染和 UI 插入标记。

## 第二阶段：数组编辑器

目标：面向数组、序列、栈、队列等竞赛教学场景。

能力：

- 修改元素值。
- 在指定位置插入元素。
- 删除元素。
- 交换两个元素。
- 拖动元素重排。
- 支持高亮区间、指针标记、当前下标标记。
- 支持从逗号分隔文本重新导入。

## 第三阶段：图编辑器

目标：接近专用图编辑器能力，服务图论教学。

能力：

- 添加/删除节点。
- 拖动节点并保持边连接。
- 添加/删除边。
- 有向边和无向边。
- 边权。
- 自环和重边策略。
- 邻接矩阵、边列表、邻接表导入/导出。
- 图布局辅助：环形、分层、网格、保持手动布局。
- 高亮节点、边、路径、连通块。

## 第四阶段：树/二叉树编辑器

目标：支持二叉树、堆、BST、线段树等教学表达。

能力：

- 添加/删除节点或子树。
- 修改节点值。
- 自动层级布局。
- 左右儿子语义。
- 遍历顺序高亮。
- 支持层序数组导入和导出。
- 后续扩展 AVL/红黑树/堆等专用样式。

## 当前执行项

已完成第一阶段底座：

1. 结构创建模块已生成 `array-structure`、`graph-structure`、`tree-structure`。
2. 白板模型默认值和归一化逻辑已支持三种结构元素。
3. Konva 已能渲染三种结构元素。
4. 图层标签已展示结构摘要。
5. 已补充结构解析、结构元素创建、模型归一化、Konva 渲染和 UI 标记测试。

当前继续推进：

1. 数组结构已支持：
   - 前插、后插、指定位置插入。
   - 指定位置删除、删除末项。
   - 指定位置改值。
   - 交换两个元素。
   - 将元素移动到指定位置。
   - 画布内拖动数组单元格重排。
   - 画布内双击数组单元格改值。
   - 区间高亮和当前指针标记。
   - 数组、栈、队列、双端队列模式标记。
   - 从结构输入框重载。
2. 图结构已支持：
   - 输入解析有向边和边权，例如 `A->B:5`、`A-B=3`。
   - 添加节点。
   - 默认连接最后两个节点。
   - 通过边文本追加边，并自动补齐缺失节点。
   - 删除末尾边。
   - 删除节点时同步删除关联边。
   - 切换后续快捷连边的默认有向/无向。
   - 画布内拖拽单个图节点，并保持边连接。
   - 点选源节点和目标节点连边。
   - 双击边编辑方向和权值。
   - 节点/边高亮。
   - 边列表、邻接表、邻接矩阵导出。
   - 邻接表、邻接矩阵导入。
   - 环形、网格、分层布局辅助。
   - 力导向布局辅助。
   - 自环和重边曲线渲染。
   - 从结构输入框重载，并尽量保留已有节点坐标。
3. 树结构已支持：
   - 层序输入重载。
   - 追加节点。
   - 给指定父节点添加左儿子或右儿子。
   - 修改指定节点值。
   - 画布内双击树节点改值。
   - 画布内点选父节点，再用面板添加左右儿子。
   - 删除指定子树。
   - 层序、前序、中序、后序遍历顺序计算和高亮显示。
   - 遍历上一步/下一步推进。
   - 子树折叠、展开、复制和移动。

## 后续体验优化

当前结构功能主线已经完成，后续更多是交互体验和专用结构扩展：

1. 数组编辑器
   - 为栈、队列、双端队列增加更明显的端点、入队/出队方向视觉提示。
2. 图编辑器
   - 更完整的图编辑专用面板，减少 `prompt` 输入。
   - 单边选中态和颜色/线宽/虚线等样式编辑。
3. 树编辑器
   - 堆、BST、线段树等专用结构样式。
   - 遍历动画播放和暂停。

## 验证

- 已补充结构纯函数测试、Konva 图节点拖拽测试、属性面板动作渲染测试。
- 每批改动继续运行：
  - `rtk npm test -- --run`
  - `rtk npm run build`
