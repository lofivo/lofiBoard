# 计算机科学结构模板开发计划

## 目标

为白板增加快速插入计算机科学常用结构图案的能力，首批支持数组、图、树。用户先选择结构类型，再填写初始结构；输入为空时使用默认内容。插入后的内容应作为一组普通白板元素存在，自动分组，兼容现有选择、移动、缩放、复制、撤销重做、保存导出和取消分组能力。

## 已确认决策

- 数据形态：不新增复杂容器元素，模板展开为现有 `rect`、`ellipse`、`line`、`arrow`、`text` 等普通元素。
- 分组：每次插入生成一个 `groupId`，默认选中整组，用户可取消分组后编辑单个元素。
- 入口：工具栏新增“结构”入口，打开结构模板面板。
- 输入方式：先选择数组、图、树，再填写文本初始结构；空输入使用默认内容。
- 数组展示：两行表格，上行是下标，下行是值。
- 输入格式：
  - 数组：逗号分隔值，例如 `1,2,3,4,5`
  - 图：边列表，例如 `A-B, A-C, B-D`
  - 树：层序数组，例如 `A, B, C, D, E, null, F`

## MVP 范围

1. 新增结构模板配置和解析逻辑。
2. 生成可视元素：
   - 数组：每个元素两格单元，上格下标、下格值。
   - 图：根据边列表生成节点和连线，节点环形布局。
   - 树：根据层序数组生成二叉树节点和父子连线，跳过 `null`。
3. 工具栏新增“结构”按钮，点击显示模板面板。
4. 面板支持结构类型切换、输入初始内容、插入、取消。
5. 插入位置优先使用上次指针所在白板坐标，否则使用当前视口中心。
6. 插入后切换到选择工具并选中新结构整组。
7. 补充解析与 UI 标记测试，运行现有测试集。

## 非目标

- 不做复杂布局配置，例如节点半径、树方向、数组行列数。
- 不做图/树节点拖拽后自动重排。
- 不做专用结构编辑器，插入后依赖现有白板对象编辑能力。
- 不引入外部图布局库。

## 实施步骤

1. 新建 `src/structures/structure-templates.js`：
   - 定义结构类型、默认输入、解析函数。
   - 提供 `createStructureElements({ type, input, point, zIndexStart })`。
   - 统一设置样式、尺寸、`groupId`、`zIndex`。
2. 更新 UI 配置：
   - 增加 `TOOLS.STRUCTURE`。
   - 工具栏显示“结构”按钮和 tooltip。
   - 提供结构模板面板的配置与 markup。
3. 更新应用壳：
   - 渲染结构模板面板。
   - 增加结构输入框、模板按钮、插入/取消按钮。
4. 更新白板应用：
   - 绑定结构面板事件。
   - 处理结构类型切换、默认 placeholder、打开/关闭面板。
   - 插入结构元素、分组、选择、推入历史。
5. 更新样式：
   - 复用当前弹出层视觉语言，为结构面板增加紧凑布局。
   - 确保面板不遮挡工具栏关键操作，移动端宽度可用。
6. 测试：
   - 单测数组、图、树解析和元素生成。
   - 单测工具栏/应用壳包含结构入口和输入面板。
   - 运行 `npm test`。

## 验收标准

- 点击“结构”可以打开面板。
- 空输入插入默认数组、图或树。
- 数组显示两行：下标和值。
- 图按边列表生成节点和边。
- 树按层序输入生成二叉树结构，`null` 节点不渲染。
- 插入后结构整体选中，撤销可移除整组。
- 保存再打开不会丢失这些结构，因为它们都是现有元素类型。
