# 结构算法可视化开发计划

## 目标

在现有结构模板基础上，为数组、二叉树、图等结构逐步增加算法可视化能力。整体交互可参考 Visualgo，但不做独立演示页，优先融入当前白板工作流。

第一阶段只实现数组相关算法，先落地冒泡排序。

## 已确认设计

- 算法可视化复用现有结构元素，不重新实现一套画布或结构渲染。
- 选中结构后在属性栏/侧边栏显示算法相关控制。
- 算法执行直接绑定并修改当前选中的数组结构。
- 运行前记录初始数组，提供“重置”能力恢复到运行前数据。
- 冒泡排序第一版只支持数字数组，暂不处理字符串字典序或自定义比较。
- 算法运行会话存在时锁定数组结构编辑，只允许播放器控制；重置或结束后恢复编辑。
- 算法步骤说明第一版显示在属性栏内，不在画布上新增浮动说明。
- 冒泡排序第一版需要交换动画；比较步骤可只做高亮，交换步骤需要可见的单元格交换过程。
- 交换动画表现为两个相邻数组单元格移动位置；动画结束后提交真实数组状态。
- 算法单步不逐条写入白板撤销历史；整个排序会话完成后作为一次历史记录。
- 算法入口放在选中结构后的属性栏中，第一阶段在数组属性栏追加“算法”控制区。
- 算法运行中切换选中对象时自动暂停并保留会话；重新选中原数组后继续显示状态。若绑定数组被删除，则清理会话。
- 非数字数组启动冒泡排序时不执行，属性栏算法区显示错误提示，原数组保持不变。
- 播放速度第一版使用连续滑杆，范围 0.5x 到 3x，默认 1x；比较间隔和交换动画时长随倍率缩放。
- 冒泡排序步骤拆分为“比较步骤”和“交换步骤”；需要交换时先停在比较说明，再单独播放交换动画。
- 冒泡排序需要标记已排序尾部区间；比较元素和已排序元素使用不同视觉状态。
- 需要扩展数组结构的算法标记能力，例如 `activeIndices`、`sortedIndices`，不能只依赖当前连续高亮区间。
- 算法运行状态不保存到白板文件；文件只保存数组当前值和普通结构状态，重新打开后不恢复播放进度。
- 冒泡排序完成后保留完成态并停止播放；用户点击“结束”后恢复普通编辑。
- 第一版不显示比较次数、交换次数、当前轮次等统计信息，只显示必要步骤进度和步骤说明。
- 第一版不显示伪代码，也不做当前执行行高亮。
- 播放器第一版包含：开始、上一步、下一步、播放/暂停、重置、结束；不做跳到开头、跳到结尾。
- 重置：保留算法会话，数组恢复到运行前状态，步骤回到第 0 步，继续锁定编辑。
- 结束：退出算法会话，清除算法高亮/指针，恢复普通编辑，数组保持当前显示状态。
- 交换动画期间禁用上一步、下一步、播放/暂停；允许重置和结束，但需要先取消动画并回到稳定状态。
- 算法步骤生成拆成独立模块，不直接堆在 `whiteboard-app.js` 中。
- 建议新增 `src/algorithms/array-algorithms.js`，负责数字校验、冒泡排序步骤生成和步骤状态描述。
- 建议新增 `tests/algorithms/array-algorithms.test.js`，覆盖数字校验、比较/交换步骤、已排序区间和重置数据等纯逻辑。

## 推荐框架

- 新增“算法”控制区，仅在选中支持算法的结构时显示。
- 控制区包含：
  - 算法选择
  - 重置
  - 上一步
  - 下一步
  - 播放/暂停
  - 速度控制
- 算法执行先生成步骤序列，每一步包含：
  - 数据快照
  - 高亮项
  - 指针位置
  - 步骤类型，例如比较、交换、完成
  - 简短说明

## 第一期：数组冒泡排序

- 支持选中数组后选择“冒泡排序”。
- 初始化时基于当前数组生成完整步骤。
- 单步前进时更新数组值、比较高亮和指针；遇到交换步骤时播放交换动画。
- 播放时按速度自动推进步骤。
- 重置时恢复运行前数组和清除算法状态。

## 实现进度

- 已新增数组算法纯逻辑模块和测试。
- 已接入数组属性栏算法控制区。
- 已实现冒泡排序开始、上一步、下一步、播放/暂停、重置、结束。
- 已实现非数字校验、算法运行锁定、切换选择暂停、交换动画、已排序区间标记。
- 已确保算法运行状态不保存到白板文件。

## 待确认问题
